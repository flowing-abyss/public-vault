type: ActionsTagsBackup
author: flowing-abyss
platformVersion: 7.0.24
pluginVersion: 2.2.3
timestamp: '2025-08-23T05:23:01.282Z'
actions:
  1742568170685-tSX5kwri:
    event: 0
    operation: 4
    data: |
      if (!item) {
          return "Item is empty";
      }

      const ratingTags = ["📥", "🟥", "todo", "🟦" ,"wip", "🟩", "done", "✖"];
      const tagToToggle = "🟦";

      const hasTag = item.getTags().some(tag => tag.tag === tagToToggle);

      item.getTags().forEach(tagObj => {
          if (ratingTags.includes(tagObj.tag)) {
              item.removeTag(tagObj.tag);
          }
      });

      if (!hasTag) {
          item.addTag(tagToToggle);
          return `[Marked as ${tagToToggle}] for: ${item.getField("title")}`;
      } else {
          return `[Removed tag ${tagToToggle}] from: ${item.getField("title")}`;
      }
    shortcut: ''
    enabled: true
    menu: status {🟦} WIP
    name: wip
    showInMenu:
      item: true
      collection: true
      tools: false
      reader: false
      readerAnnotation: false
  1742567875638-tSX5kwri:
    event: 0
    operation: 4
    data: |
      if (!item) {
          return "Item is empty";
      }

      const ratingTags = ["📥", "🟥", "todo", "🟦" ,"wip", "🟩", "done", "✖"];
      const tagToToggle = "🟥";

      const hasTag = item.getTags().some(tag => tag.tag === tagToToggle);

      item.getTags().forEach(tagObj => {
          if (ratingTags.includes(tagObj.tag)) {
              item.removeTag(tagObj.tag);
          }
      });

      if (!hasTag) {
          item.addTag(tagToToggle);
          return `[Marked as ${tagToToggle}] for: ${item.getField("title")}`;
      } else {
          return `[Removed tag ${tagToToggle}] from: ${item.getField("title")}`;
      }
    shortcut: ''
    enabled: true
    menu: status {🟥} ToDo
    name: todo
    showInMenu:
      item: true
      collection: true
      tools: false
      reader: false
      readerAnnotation: false
  1746856371893-tSX5kwri:
    event: 1
    operation: 1
    data: 📥
    shortcut: ''
    enabled: true
    menu: 📥
    name: new
    showInMenu:
      item: false
      collection: false
      tools: false
      reader: false
      readerAnnotation: false
  1742567774865-tSX5kwri:
    event: 0
    operation: 4
    data: |
      if (!item) {
          return "Item is empty";
      }

      const ratingTags = ["📥", "🟥", "todo", "🟦" ,"wip", "🟩", "done", "✖"];
      const tagToToggle = "📥";

      const hasTag = item.getTags().some(tag => tag.tag === tagToToggle);

      item.getTags().forEach(tagObj => {
          if (ratingTags.includes(tagObj.tag)) {
              item.removeTag(tagObj.tag);
          }
      });

      if (!hasTag) {
          item.addTag(tagToToggle);
          return `[Marked as ${tagToToggle}] for: ${item.getField("title")}`;
      } else {
          return `[Removed tag ${tagToToggle}] from: ${item.getField("title")}`;
      }
    shortcut: ''
    enabled: true
    menu: status {📥} Inbox
    name: inbox
    showInMenu:
      item: true
      collection: true
      tools: false
      reader: false
      readerAnnotation: false
  1744268580135-tSX5kwri:
    event: 0
    operation: 3
    data: ⭐
    shortcut: ''
    enabled: true
    menu: _⭐ Favorite
    name: favorite
    showInMenu:
      item: true
      collection: true
      tools: true
      reader: false
      readerAnnotation: false
  1742568268591-tSX5kwri:
    event: 0
    operation: 4
    data: |
      if (!item) {
          return "Item is empty";
      }

      const ratingTags = ["📥", "🟥", "todo", "🟦" ,"wip", "🟩", "done", "✖"];
      const tagToToggle = "✖";

      const hasTag = item.getTags().some(tag => tag.tag === tagToToggle);

      item.getTags().forEach(tagObj => {
          if (ratingTags.includes(tagObj.tag)) {
              item.removeTag(tagObj.tag);
          }
      });

      if (!hasTag) {
          item.addTag(tagToToggle);
          return `[Marked as ${tagToToggle}] for: ${item.getField("title")}`;
      } else {
          return `[Removed tag ${tagToToggle}] from: ${item.getField("title")}`;
      }
    shortcut: ''
    enabled: true
    menu: status {✖} Drop
    name: drop
    showInMenu:
      item: true
      collection: true
      tools: false
      reader: false
      readerAnnotation: false
  1742568214801-tSX5kwri:
    event: 0
    operation: 4
    data: |
      if (!item) {
          return "Item is empty";
      }

      const ratingTags = ["📥", "🟥", "todo", "🟦" ,"wip", "🟩", "done", "✖"];
      const tagToToggle = "🟩";

      const hasTag = item.getTags().some(tag => tag.tag === tagToToggle);

      item.getTags().forEach(tagObj => {
          if (ratingTags.includes(tagObj.tag)) {
              item.removeTag(tagObj.tag);
          }
      });

      if (!hasTag) {
          item.addTag(tagToToggle);
          return `[Marked as ${tagToToggle}] for: ${item.getField("title")}`;
      } else {
          return `[Removed tag ${tagToToggle}] from: ${item.getField("title")}`;
      }
    shortcut: ''
    enabled: true
    menu: status {🟩} Done
    name: done
    showInMenu:
      item: true
      collection: true
      tools: false
      reader: false
      readerAnnotation: false
  1753250549012-tSX5kwri:
    event: 0
    operation: 4
    data: |
      if (!item) {
          return "Item is empty";
      }

      const ratingTags = ["🌕", "🌔", "🌓", "🌒", "🌑"];
      const tagToToggle = "🌕";

      const hasTag = item.getTags().some(tag => tag.tag === tagToToggle);

      item.getTags().forEach(tagObj => {
          if (ratingTags.includes(tagObj.tag)) {
              item.removeTag(tagObj.tag);
          }
      });

      if (!hasTag) {
          item.addTag(tagToToggle);
          return `[Marked as ${tagToToggle}] for: ${item.getField("title")}`;
      } else {
          return `[Removed tag ${tagToToggle}] from: ${item.getField("title")}`;
      }
    shortcut: ''
    enabled: true
    menu: rating (5/5) 🌕 Masterpiece
    name: 🌕 Masterpiece
    showInMenu:
      item: true
      collection: true
      tools: false
      reader: false
      readerAnnotation: false
  1753250589233-tSX5kwri:
    event: 0
    operation: 4
    data: |
      if (!item) {
          return "Item is empty";
      }

      const ratingTags = ["🌕", "🌔", "🌓", "🌒", "🌑"];
      const tagToToggle = "🌔";

      const hasTag = item.getTags().some(tag => tag.tag === tagToToggle);

      item.getTags().forEach(tagObj => {
          if (ratingTags.includes(tagObj.tag)) {
              item.removeTag(tagObj.tag);
          }
      });

      if (!hasTag) {
          item.addTag(tagToToggle);
          return `[Marked as ${tagToToggle}] for: ${item.getField("title")}`;
      } else {
          return `[Removed tag ${tagToToggle}] from: ${item.getField("title")}`;
      }
    shortcut: ''
    enabled: true
    menu: rating (4/5) 🌔 Great
    name: 🌔 Great
    showInMenu:
      item: true
      collection: true
      tools: false
      reader: false
      readerAnnotation: false
  1753250656754-tSX5kwri:
    event: 0
    operation: 4
    data: |
      if (!item) {
          return "Item is empty";
      }

      const ratingTags = ["🌕", "🌔", "🌓", "🌒", "🌑"];
      const tagToToggle = "🌓";

      const hasTag = item.getTags().some(tag => tag.tag === tagToToggle);

      item.getTags().forEach(tagObj => {
          if (ratingTags.includes(tagObj.tag)) {
              item.removeTag(tagObj.tag);
          }
      });

      if (!hasTag) {
          item.addTag(tagToToggle);
          return `[Marked as ${tagToToggle}] for: ${item.getField("title")}`;
      } else {
          return `[Removed tag ${tagToToggle}] from: ${item.getField("title")}`;
      }
    shortcut: ''
    enabled: true
    menu: rating (3/5) 🌓 Good
    name: 🌓 Good
    showInMenu:
      item: true
      collection: true
      tools: false
      reader: false
      readerAnnotation: false
  1753250731264-tSX5kwri:
    event: 0
    operation: 4
    data: |-
      if (!item) {
          return "Item is empty";
      }

      const ratingTags = ["🌕", "🌔", "🌓", "🌒", "🌑"];
      const tagToToggle = "🌒";

      const hasTag = item.getTags().some(tag => tag.tag === tagToToggle);

      item.getTags().forEach(tagObj => {
          if (ratingTags.includes(tagObj.tag)) {
              item.removeTag(tagObj.tag);
          }
      });

      if (!hasTag) {
          item.addTag(tagToToggle);
          return `[Marked as ${tagToToggle}] for: ${item.getField("title")}`;
      } else {
          return `[Removed tag ${tagToToggle}] from: ${item.getField("title")}`;
      }
    shortcut: ''
    enabled: true
    menu: rating (2/5) 🌒 Mediocre
    name: 🌒 Mediocre
    showInMenu:
      item: true
      collection: true
      tools: false
      reader: false
      readerAnnotation: false
  1753250765159-tSX5kwri:
    event: 0
    operation: 4
    data: |-
      if (!item) {
          return "Item is empty";
      }

      const ratingTags = ["🌕", "🌔", "🌓", "🌒", "🌑"];
      const tagToToggle = "🌑";

      const hasTag = item.getTags().some(tag => tag.tag === tagToToggle);

      item.getTags().forEach(tagObj => {
          if (ratingTags.includes(tagObj.tag)) {
              item.removeTag(tagObj.tag);
          }
      });

      if (!hasTag) {
          item.addTag(tagToToggle);
          return `[Marked as ${tagToToggle}] for: ${item.getField("title")}`;
      } else {
          return `[Removed tag ${tagToToggle}] from: ${item.getField("title")}`;
      }
    shortcut: ''
    enabled: true
    menu: rating (1/5) 🌑 Poor
    name: 🌑 Poor
    showInMenu:
      item: true
      collection: true
      tools: false
      reader: false
      readerAnnotation: false
  1753249689104-tSX5kwri:
    event: 0
    operation: 4
    data: |-
      if (!item) {
          return "Item is empty";
      }

      const ratingTags = ["🅰️", "🅱️", "👓", "📢", "💬"];
      const tagToToggle = "💬";

      const hasTag = item.getTags().some(tag => tag.tag === tagToToggle);

      item.getTags().forEach(tagObj => {
          if (ratingTags.includes(tagObj.tag)) {
              item.removeTag(tagObj.tag);
          }
      });

      if (!hasTag) {
          item.addTag(tagToToggle);
          return `[Marked as ${tagToToggle}] for: ${item.getField("title")}`;
      } else {
          return `[Removed tag ${tagToToggle}] from: ${item.getField("title")}`;
      }
    shortcut: ''
    enabled: true
    menu: "priority (🇪) 💬\_Unverified / Opinion"
    name: "🇪\_Unverified / Opinion"
    showInMenu:
      item: true
      collection: true
      tools: false
      reader: false
      readerAnnotation: false
  1753249656048-tSX5kwri:
    event: 0
    operation: 4
    data: |-
      if (!item) {
          return "Item is empty";
      }

      const ratingTags = ["🅰️", "🅱️", "👓", "📢", "💬"];
      const tagToToggle = "📢";

      const hasTag = item.getTags().some(tag => tag.tag === tagToToggle);

      item.getTags().forEach(tagObj => {
          if (ratingTags.includes(tagObj.tag)) {
              item.removeTag(tagObj.tag);
          }
      });

      if (!hasTag) {
          item.addTag(tagToToggle);
          return `[Marked as ${tagToToggle}] for: ${item.getField("title")}`;
      } else {
          return `[Removed tag ${tagToToggle}] from: ${item.getField("title")}`;
      }
    shortcut: ''
    enabled: true
    menu: "priority (🇩) 📢\_Popular Science / Journalism"
    name: "🇩\_Popular Science"
    showInMenu:
      item: true
      collection: true
      tools: false
      reader: false
      readerAnnotation: false
  1753249618736-tSX5kwri:
    event: 0
    operation: 4
    data: |-
      if (!item) {
          return "Item is empty";
      }

      const ratingTags = ["🅰️", "🅱️", "👓", "📢", "💬"];
      const tagToToggle = "👓";

      const hasTag = item.getTags().some(tag => tag.tag === tagToToggle);

      item.getTags().forEach(tagObj => {
          if (ratingTags.includes(tagObj.tag)) {
              item.removeTag(tagObj.tag);
          }
      });

      if (!hasTag) {
          item.addTag(tagToToggle);
          return `[Marked as ${tagToToggle}] for: ${item.getField("title")}`;
      } else {
          return `[Removed tag ${tagToToggle}] from: ${item.getField("title")}`;
      }
    shortcut: ''
    enabled: true
    menu: "priority (🇨) 👓\_Expert / Industry"
    name: "🇨\_Expert / Industry"
    showInMenu:
      item: true
      collection: true
      tools: false
      reader: false
      readerAnnotation: false
  1753249577689-tSX5kwri:
    event: 0
    operation: 4
    data: |-
      if (!item) {
          return "Item is empty";
      }

      const ratingTags = ["🅰️", "🅱️", "👓", "📢", "💬"];
      const tagToToggle = "🅱️";

      const hasTag = item.getTags().some(tag => tag.tag === tagToToggle);

      item.getTags().forEach(tagObj => {
          if (ratingTags.includes(tagObj.tag)) {
              item.removeTag(tagObj.tag);
          }
      });

      if (!hasTag) {
          item.addTag(tagToToggle);
          return `[Marked as ${tagToToggle}] for: ${item.getField("title")}`;
      } else {
          return `[Removed tag ${tagToToggle}] from: ${item.getField("title")}`;
      }
    shortcut: ''
    enabled: true
    menu: "priority (🇧) 🅱️\_Secondary Research"
    name: "🇧\_Secondary Research"
    showInMenu:
      item: true
      collection: true
      tools: false
      reader: false
      readerAnnotation: false
  1753249469766-tSX5kwri:
    event: 0
    operation: 4
    data: |-
      if (!item) {
          return "Item is empty";
      }

      const ratingTags = ["🅰️", "🅱️", "👓", "📢", "💬"];
      const tagToToggle = "🅰️";

      const hasTag = item.getTags().some(tag => tag.tag === tagToToggle);

      item.getTags().forEach(tagObj => {
          if (ratingTags.includes(tagObj.tag)) {
              item.removeTag(tagObj.tag);
          }
      });

      if (!hasTag) {
          item.addTag(tagToToggle);
          return `[Marked as ${tagToToggle}] for: ${item.getField("title")}`;
      } else {
          return `[Removed tag ${tagToToggle}] from: ${item.getField("title")}`;
      }
    shortcut: ''
    enabled: true
    menu: "priority (🇦) 🅰️\_Primary Research"
    name: "🇦\_Primary Research"
    showInMenu:
      item: true
      collection: true
      tools: false
      reader: false
      readerAnnotation: false
